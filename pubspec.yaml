name: rss_book
description: Read RSS feeds like it would be a good book
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  google_fonts:

  multiple_screens:

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true